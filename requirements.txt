# DealPilot Dependencies
# วันที่ 1: Foundation
google-adk>=0.5.0
google-generativeai>=0.8.0
python-dotenv>=1.0.0

# Data & Models
pydantic>=2.0.0

# Web UI (วันที่ 5)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.0

# Security (วันที่ 5)
google-cloud-secret-manager>=2.20.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# MCP Tools (วันที่ 2-4)
# google-cloud-aiplatform  # uncomment เมื่อใช้ MCP จริง

# Google APIs integration (วันที่ 4)
google-api-python-client>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
